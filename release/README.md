# Release Artifacts

Windows executables and checksums are published through GitHub Releases.
They are intentionally excluded from source control.

CI on this branch also builds a portable tryout ZIP as a workflow artifact
named `SwitchRefreshConfigurationImportTool-visual-port-map-windows-x64`.
That artifact is for local review of the Interactive Port Map. It is not a
numbered GitHub Release.
